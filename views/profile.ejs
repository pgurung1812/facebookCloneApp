<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>Hello <%=user.userName %></h1>
    <h2>profile email:<%=user.email %></h2>

    <% for(let i=0;i< posts.length;i++) { %> <%= posts[i].title %> <%=
    posts[i].message %> <% } %>

    <form action="/profile/posts" method="post">
      <label for="title">Title:</label>
      <input type="text" name="title" placeholder="Please enter title here" />
      <label for="post">Post:</label>
      <input type="text" name="post" placeholder="Please enter Post here" />
      <button type="submit">Add a post</button>
    </form>

    <h2>Add Image as post</h2>
    <form
      action="/profile/posts/images"
      enctype="multipart/form-data"
      method="POST"
    >
      <label for="title">Title</label>
      <input type="text" id="title" name="title" />

      <label for="caption">Caption</label>
      <textarea id="caption" name="caption"></textarea>

      <label for="imgUpload">Image</label>
      <input type="file" id="imageUpload" name="file" />

      <button type="submit" value="Upload">Submit</button>
    </form>
  </body>
</html>
